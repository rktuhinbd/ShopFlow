# ShopFlow — Documentation Rule

- All project knowledge lives in `docs/`
- Task files in `tasks/{planned,in-progress,done,blocked}/`
- Update status docs after every work session
- Commit messages: `type(scope): description`
